# =============================================================================
# Docker Compose Production Overrides
# =============================================================================
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# First-time SSL setup:
#   1. Set PRODUCTION_DOMAIN in .env
#   2. Point DNS to your server
#   3. Start with HTTP-only nginx first to get certs:
#      cp docker/nginx/nginx.dev.conf docker/nginx/nginx.conf.tmp
#      docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#   4. Run certbot:
#      docker-compose -f docker-compose.yml -f docker-compose.prod.yml \
#        run --rm certbot certonly --webroot -w /var/www/certbot \
#        -d ${PRODUCTION_DOMAIN} --agree-tos -m admin@${PRODUCTION_DOMAIN}
#   5. Restore HTTPS nginx config and restart:
#      docker-compose -f docker-compose.yml -f docker-compose.prod.yml restart nginx
# =============================================================================

services:
  postgres:
    # Stricter resource limits for production
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Enhanced logging for production
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"

  redis:
    # Production memory settings
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -e --no-auth-warning -a \"${REDIS_PASSWORD}\" ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===========================================================================
  # Nginx Reverse Proxy + TLS Termination
  # ===========================================================================
  nginx:
    image: nginx:alpine
    container_name: grader_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - PRODUCTION_DOMAIN=${PRODUCTION_DOMAIN}
    volumes:
      # Nginx template — envsubst replaces ${PRODUCTION_DOMAIN} at startup
      - ./docker/nginx/nginx.conf:/etc/nginx/templates/default.conf.template:ro
      # Let's Encrypt certificates + webroot
      - certbot_certs:/etc/letsencrypt:ro
      - certbot_webroot:/var/www/certbot:ro
      # Frontend production build (mount your built files here)
      - ./frontend/dist:/usr/share/nginx/html:ro
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:80/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - grader_network
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"

  # ===========================================================================
  # Certbot — Let's Encrypt certificate management
  # ===========================================================================
  certbot:
    image: certbot/certbot
    container_name: grader_certbot
    volumes:
      - certbot_certs:/etc/letsencrypt
      - certbot_webroot:/var/www/certbot
    # Only run on-demand: docker-compose run --rm certbot renew
    profiles:
      - ssl

  # ===========================================================================
  # FastAPI Backend Application
  # ===========================================================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grader_backend
    restart: unless-stopped
    
    env_file:
      - .env
    
    # Backend is NOT exposed externally — nginx proxies to it
    ports: []
    
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    volumes:
      - ./data:/app/data
      - ./exports:/app/exports
      - ./logs:/app/logs
    
    networks:
      - grader_network

  # ===========================================================================
  # Celery Workers — Production overrides
  # ===========================================================================
  worker-rag:
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '2'
        reservations:
          cpus: '0.5'

  worker-llm:
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '1'
        reservations:
          cpus: '0.25'

  worker-canvas:
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '1'
        reservations:
          cpus: '0.25'

  # Flower — internal only, access via SSH tunnel: ssh -L 5555:localhost:5555 root@VPS
  flower:
    env_file:
      - .env
    command: celery -A backend.celery_app flower --port=5555 --broker=redis://:${REDIS_PASSWORD}@redis:6379/0

  # ===========================================================================
  # Frontend Build — one-shot service to produce frontend/dist
  # ===========================================================================
  frontend-build:
    image: node:20-alpine
    container_name: grader_frontend_build
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: sh -c "npm ci && npm run build"
    profiles:
      - build

volumes:
  certbot_certs:
    driver: local
    name: grader_certbot_certs
  certbot_webroot:
    driver: local
    name: grader_certbot_webroot
